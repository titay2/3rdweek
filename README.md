# mongoose-start

* Clone this repo
* Run `npm install`
* Create config.js:
```javascript
const config = {};

config.user = 'username';
config.pwd = 'password';

module.exports = config;
```
* run `npm run watch`